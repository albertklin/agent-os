# Server Configuration
PORT=3011

# Database
DB_PATH=./agent-os.db

# Network Security
# The server REQUIRES Tailscale and binds ONLY to the Tailscale interface.
# This ensures the server is only accessible from your Tailscale network.
# If Tailscale is not running, the server will refuse to start.
